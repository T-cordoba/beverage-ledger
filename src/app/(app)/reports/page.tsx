import { ConsumptionReportView } from '@/features/reports';

export const metadata = { title: 'Reports · Beverage Ledger' };

export default function ReportsPage() {
  return <ConsumptionReportView />;
}
